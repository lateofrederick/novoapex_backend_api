# TLS is provisioned automatically for $DOMAIN. Everything — API, webhooks,
# the /admin/queues dashboard and /api-docs — is served by go-api, which trusts
# this single proxy hop for client IPs (X-Forwarded-For).
{$DOMAIN} {
	encode zstd gzip
	reverse_proxy go-api:3100
}
