{
	{$DOMAIN}
}

# Queue dashboard: auth enforced by the asynqmon-auth sidecar
# (BULL_BOARD_USER / BULL_BOARD_PASSWORD from .env).
/admin/queues* {
	reverse_proxy asynqmon-auth:8081
}

handle {
	reverse_proxy go-api:3100
}
